services:
  - type: web
    name: piston-api
    env: docker
    plan: free
    repo: https://github.com/<your-username>/piston
    branch: master
    dockerfilePath: ./Dockerfile
    autoDeploy: true
